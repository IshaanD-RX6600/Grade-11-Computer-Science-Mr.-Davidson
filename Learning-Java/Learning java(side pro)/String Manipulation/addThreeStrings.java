public class addThreeStrings {
    public static int sumStrings(String one, String two, String three)
{
    int sum = Integer.parseInt(one) + Integer.parseInt(two) + Integer.parseInt(three);
    return sum;
    //We will use the parse int to return 13
}
}
