public static findingTheKey{
  public boolean phraseContainsKey(String phrase, String key) {
    return phrase.contains(key);
    /*
    The method takes 2 parameters, phrase and key. 
    It uses the contains.(); method to check whether or not the key is there or not
    If the key is found within the phrase, it returns true.
    If the key is not found it will return false
    */
  }
}
